// import { addToCard } from './shoppingCard.js';

// addToCard();

import { cloneDeep } from 'lodash-es';

const state = {
    cart :[
        {product:'bread', quan = 4},
        {product:'brad', quan = 5}

    ]
}